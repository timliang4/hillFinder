import { AccessToken, RefreshTokenRequest } from './types';
import { Oauth } from './resources/oauth';
declare type RequestParams = {
    query?: Record<string, any>;
    body?: Record<string, any> | any;
    headers?: Record<string, any>;
    access_token?: string;
};
export declare class Request {
    readonly config: RefreshTokenRequest;
    private token?;
    readonly oauth: Oauth;
    constructor(config: RefreshTokenRequest, token?: AccessToken | undefined);
    private getAccessToken;
    makeApiRequest<Response>(method: string, uri: string, params?: RequestParams): Promise<Response>;
}
export {};
